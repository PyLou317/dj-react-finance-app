server {
    listen 80;
    server_name localhost;

    # This should point to your build output folder, e.g., /app/dist
    root /app/frontend/dist; 
    index index.html;

    location / {
        # Try to serve the file directly, then fall back to index.html
        try_files $uri $uri/ /index.html;
    }
}